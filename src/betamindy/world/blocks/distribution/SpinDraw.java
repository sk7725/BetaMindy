package betamindy.world.blocks.distribution;

/** Draws itself instead of just its icon when picked up by spinners. */
public interface SpinDraw {
    void drawSpinning(float x, float y, float dr);
}
