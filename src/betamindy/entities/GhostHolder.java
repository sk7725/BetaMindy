package betamindy.entities;

import mindustry.gen.*;

/**
 * An interface that holds a drawing & updating ghost extension.
 * @author GlennFolker
 * @author sunny
 */
public interface GhostHolder {
    void drawGhost();
    void updateGhost();
}
